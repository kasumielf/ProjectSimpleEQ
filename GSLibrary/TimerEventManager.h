#pragma once
#include <list>
#include <chrono>
/*
class TimerEventManager
{
private:
	std::list<TimeEvent> m_timerEventQueue;

public:
	TimerEventManager();
	~TimerEventManager();

	void Enqueue(const double duration, const Event event);
	const TimeEvent Dequeue();
	bool isEmpty() { return m_timerEventQueue.size() <= 0 ? true : false; }
};
*/
